{% extends "_layout.html.twig" %}

{% block content %}

	{% for product in products |slice(0,3) %}
		{{ product }}
	{% endfor %}
	<br>

	{% for product in products |slice(3,6) %}
		{{ product }}
	{% endfor %}

	{{ dump(products)}}

{% endblock %}
